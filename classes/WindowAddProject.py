from tkinter import *
from tkinter.ttk import *

class WindowAddScript(Frame):
    def __init__(self,master=None):
        Frame.__init__(self,master)
        pass

    def createWidgets():
        #Label (Project Name)
        #Textbox, one line
        #Label (Paste p roject from clipboard, optional)
        #Textbox, multi line, with scrollbar
        #Checkbox, with Label (Open this project in IDLE after creation)
        #Button (OK), Button (Cancel)
        pass
